# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: EgressDapr
services:
    egressdapr:
        resourceName: caegressdapr
        project: src/EgressDapr
        dist: build
        language: csharp
        host: containerapp