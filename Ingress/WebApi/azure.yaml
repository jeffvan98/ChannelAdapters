# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: WebApi
services:
    ingresswebapi:
        resourceName: caingresswebapi
        project: src/IngressWebApi
        dist: build
        language: csharp
        host: containerapp
